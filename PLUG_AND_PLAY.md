# PLUG_AND_PLAY — ternary-renormalize

> *Integration guide.*

## Dependency

```toml
[dependencies]
ternary_renormalize = "0.1.0"
```

## Feature Flags

| Feature | Default | Description |
|---------|---------|-------------|
| `std` | yes | Standard library |
| `alloc` | yes | Allocator support |

## Integration

Import `ternary_renormalize` in your project to access the functionality.

```
use ternary_renormalize::{};
```

## Compatibility

- **Rust edition**: 2021+
- **Targets**: All tier-1 Rust targets
